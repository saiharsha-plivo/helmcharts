apiVersion: v1
kind: Service
metadata:
  name: {{ include "s3bucket.fullname" . }}
  labels:
    {{- include "s3bucket.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "s3bucket.selectorLabels" . | nindent 4 }}
